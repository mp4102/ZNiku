# ZNIKU v0.3.6 自动化验证记录

本记录分开记录实现、合成验证和操作者验收，不把自动化通过等同于真实长片交付。
环境：Windows、Python 3.12.13、FFmpeg 8.1.2。仅使用新建合成目录；未改动真实工程、NAS 媒体或旧服务。

## Phase 1 / PERF-001

已实现：完整增强章直接引用，只创建邻章边界 part；对普通单轨、已知常量索引、已验证 rational FPS 的
ProRes MOV 使用有界 seek/读取停止。fractional FPS、首末帧、单帧区间及跨多个短邻章均有测试。
fragmented、非零 edit、未知索引/时钟等保留原安全路线；不扩大旧 exact 的接受集合。

三章各 600 帧、320×180、32 帧上下文的合成对照（媒体逻辑 AVIO，不是物理网络流量）：

| 指标 | 旧路线 | 快路径 |
| --- | ---: | ---: |
| 读取 | 77,029,245 B | 22,929,757 B |
| 写入 | 41,342,220 B | 22,626,651 B |
| 合计 | 118,371,465 B | 45,556,408 B |

逻辑读写减少 **61.51%**，全部输出 packet hash 等价。两轮短样本旧/新耗时为 0.810/0.780 s、
0.827/0.789 s；未清空 OS/NAS 缓存，不据此承诺长片加速倍数。
物理网络流量、NAS 峰值空间及长片耗时：未测量。正式 context 仍真实物化，未自动删除任何文件。

隔离暂存快照门禁：Python 专项 204 passed；mypy 238 files；Ruff/format、Schema 检查通过；
Studio typecheck、870 tests、production build 通过。进度 stage 为可选展示数据，旧响应无 stage 仍有效。
操作者真实验收：待 Phase 5。

## Phase 2 / PERF-002

已实现独立 `program-fused@0.3.6` / `final-publish-fused@0.3.6`，使用独立成果 namespace。
有序原始 FI 输入通过普通 Edge 直接进入融合节点；从已验收 metadata 重建各章半开区间，
单个顺序 concat 解码流按帧序号选择有效帧，重建 exact 时钟，只对全片末尾复制一帧，再连续编码一次。
选择表达式以平衡树写入 attempt 内受控 filter 文件，不受 Windows 命令行长度限制，也不并开一千个 decoder。
全片帧数、顺序、Main10 配方和原音轨封装沿用已有职责；不伪造裁边 Artifact。

新建向导需显式选择“0.3.6 融合编码候选（待验收）”，默认仍为已验收 0.3.5。
“额外导出裁后章节”默认关闭；开启后创建真实普通 Crop 分支，额外存储与 I/O 由界面说明。
已有工程、旧 exact producer/consumer 和历史 snapshot 不改写。

测试证据：

- 20 组逐帧 hash/PTS 对照覆盖四种源 FPS 的 2×、普通/异时基/fragmented/非零起始 PTS/等价小数 FPS。
- 实际执行 1,000 段顺序 concat 和平衡选择表达式，1,001 个结果帧的内容、顺序与时钟正确。
- 1/3/26/1,000 章、两种导出开关的 Graph 构造、保存/重开；旧图和旧接受集合保持不变。
- 12 帧、三章、带原音轨的真实合成媒体全链完成并可复用；可选 Crop 有真实文件和 Artifact。
- 故障/取消注入保留 raw，Program/Final 不登记部分成果；旧 Final 拒绝新 namespace。
- 融合默认分支未生成完整 crop/timescale MOV。实际 NAS 释放量与长片耗时仍未测量。

单遍 packet-copy 时基实验（FFmpeg 8.1.2，输入 timescale 12,000,000，短 ProRes）：

| 输出 FPS | 单遍帧/时钟等价 | 正式可选导出 |
| --- | --- | --- |
| 48、60 | 本轮合成通过 | 仍保留原安全双遍，不扩展为通用承诺 |
| 48000/1001、60000/1001 | 未通过 exact 时钟 oracle | 安全双遍通过 |

按有界实验止损：本版不启用单遍导出。融合编码不依赖该实验的成功。
隔离暂存快照：Python 两组 140 + 24 passed；mypy、Ruff/format、Schema、四组 corpus/preview 通过；
Studio 875 tests、typecheck、production build 通过。以上不是实际 Aion 或 GPU 长片验收。

后续远端漂移检查发现检入 corpus 的新增条目顺序不符，已使用原生成器完整重建并在 `96eadd5` 修正；
不是修改节点语义。所有后续门禁同时核对实际检入资产，不能只引用隔离快照通过。

## Phase 3 / DATA-001

已实现工程数据维护入口：按需扫描四类占用，默认不选文件，选中内部中转后必须勾选不可恢复并显式确认。
服务只接收短期票据及候选 ID，不接收任意删除路径。网络结果不明不自动重试，逐项报告删除/跳过/失败，
大小是逻辑字节，不声称 NAS 快照会同步释放物理空间。

清理资格只限已绑定 attempt 内、known-exact context/crop 产生的范围/时基中转。
新 `scratch-index.json` 仅记录相对位置、职责及低成本文件身份，是非权威维护提示：缺失或写失败只失去
清理资格，不阻断媒体完成，不成为 Artifact、receipt 或新执行权威。旧未知文件不按名称追认。
扫描对目录条目、深度、引用 JSON/ffconcat 均有预算；截断时整次预览不提供删除候选。

确认重验会话、版本、Run 终态、attempt 归属、索引/文件身份和所有正式引用；票据一次性且五分钟失效。
活动/等待 Run、外部来件、已登记历史成果、声明输出、日志、目录/文件链接、共享硬链接及未知文件均保留。
复用已有维护互斥，不重建 Runtime、不修改 Graph/Run/Artifact/Result，也不改变 reuse 结论。

合成测试包含缺失/变化身份、换会话/CAS、过期、预览中断、部分权限失败、一次性重放拒绝、历史引用、
ffconcat 合法空白与未知指令拒绝、有界空目录遍历及真实短 ProRes 索引 hook。
隔离暂存快照 Python 210 passed / 1 skipped；mypy 250 files、Ruff/format、Schema/corpus 通过；
Studio typecheck、886 tests、production build 通过。跳过项仅为 Windows 无符号链接创建权限，
已有模拟 reparse 及硬链接拒绝用例通过。没有执行真实数据清理。

## Phase 4 / UX-001～003

章级匹配由 Python 生成唯一建议，支持规范名与明确的片名/章/leaf 编号及 `_slp` 后缀；
乱序选择不改变身份，重复、跨章、未知后缀与一源多用均不猜测。五份乱序建议和 0002/0010 有独立测试。
界面默认选中唯一建议，允许逐项修改或跳过；预览和取消没有媒体副作用，检查与 Submit 仍独立显式执行。

收件窗口分开说明处理段、选用结果、接收后名称、收件位置和检查通过后的位置；完整路径默认折叠。
复制保留原件、当前收件目录内整理、无需复制/更名三种情况按服务事实显示，不指引往 outputs 手工投放。

常驻任务摘要、节点和右侧使用同绑定的活动展示，身份包含工程会话、Run、NodeRun、attempt 与 handoff。
本地请求只说等待响应；媒体复用正式进度，单件收纳复用既有 job 字节测量，不新增轮询。
时间分别标明本机请求等待或既有服务起时，复用 Workspace 更新，不把本机等待算成媒体处理耗时。
复制/媒体到 100% 仍等正式完成确认；外部待交付不旋转、不倒计时；断线、迟到响应不自动重放副作用。
只读查看及画布平移继续可用；读屏 live 文本不逐百分比播报，减少动态效果偏好有效。

生产浏览器合成回归覆盖 `_slp` 自动选中、取消后重选、分批/错误交付、重开、完整检查与一次 Submit；
对检查/提交请求人为延迟，再放行到真实合成服务，验证三处绑定一致、没有假百分比、无重复请求。
已有布局测试补充真实节点消费 completed 的前置断言，避免把 API 终态误当作画布已完成更新；
没有使用 force-click、放宽几何断言或重复重试掩盖预览正常失效。

只读目录预览连续 20 次，从捕获 click 到真实摘要可见后的下一渲染帧，p95 **32.4 ms**（目标 ≤150 ms）；
实际发出 20 次预览、0 次收件/检查/提交。该指标是前端请求反馈，不是 NAS 复制或检查吞吐。
完整 Studio typecheck、900 tests（78 files）及 production build 通过。
固定同一 production build 后依次运行浏览器门禁：主套件 30 passed、B 几何套件 8 passed、
C 交回/恢复套件 6 passed，共 44 项全部通过。内建无障碍检查没有 critical/serious 违规。
首轮 B 曾因并行重建 dist 导致资源 404；停止并行构建后完整顺序重跑通过，没有放宽测试或忽略 HTTP 错误。

当前 Chrome 扩展打开独立合成页返回 `ERR_BLOCKED_BY_CLIENT`，已关闭本次新建失败标签。
未降低浏览器/网络安全设置，未操作已有真实标签或服务；生产浏览器门禁使用仓库既有的隔离 Chromium。
该自动化不替代操作者的真实 Chrome、外部 AI、长片和 NAS 性能验收。

## Phase 0～4 综合门禁口径

全量 Python 运行：2172 passed / 5 skipped，1294.79 s。启动后追加/整理的匹配与收件测试另跑 29 passed，
进度专项另跑 53 passed；不把重叠的专项数量累加成全量总数。
五项跳过均因 Windows 用户未获创建符号链接权限（交回 4 项、维护 1 项），未修改系统权限；
路径越界、模拟 reparse、硬链接和引用保护的其他用例通过。

四条短合成 smoke：基础媒体（12 帧）、AV27（24→48 帧、双音轨）、Overlap（18→36 帧、三章），
Source Aligned MR-off（30/1、18→36 帧、三章）全部通过。外部步骤由合成文件显式交回，未调用真实 AI。
mypy 251 个源文件、Ruff/format、锁文件、Project Service Schema、四组 corpus/preview 漂移检查及
源准入 63 场景（39 接受/24 拒绝）通过；几何前置 31 项、独立 SVG 审计 6 项、npm audit 0 漏洞。

截至 Phase 0～4 提交，没有进入 Phase 5，没有构建/替换 Windows 交付包，没有改默认模板、迁移旧工程或清理真实文件。
真实 NAS 物理吞吐、释放量、工作目录峰值与长章时间仍需授权后测量。

阶段提交：Phase 0 `9c07859`、Phase 1 `4d91e8d`、Phase 2 `08feb26`（生成资产顺序修正 `96eadd5`）、
Phase 3 `f62a450`；Phase 4 为包含本记录的提交。每阶段已按用户授权推送 `codex/v0.3.6`。
远端 Studio CI 在 `96eadd5` 和 `f62a450` 已通过；本阶段新提交将触发独立 CI，其最终状态以 GitHub 为准，
不以早期提交绿灯替代本阶段门禁。

## Phase 5 / Windows 本机候选交付

2026-09-23 用户授权进入 Phase 5、交付启动包并 commit/push，真实全流程由用户执行。
运行代码基点 `473dc15` 的 Core CI `35836186871` 与 Studio CI `35836186861` 均已通过。
本阶段不修改运行代码或默认模板，新增融合候选浏览器用例及打包说明回归，交付记录见
[Windows 候选与验收](v0.3.6-desktop-acceptance.md)。

- 桌面构建/审计/启动/端口/Job Object/候选通道专项：101 passed。
- Studio：typecheck、900 tests（78 files）与 production build 通过。
- Ruff check、318 files format check、mypy 251 files、锁文件检查、Project Service Schema 及四组
  Presentation corpus/preview 漂移检查通过。
- 两项新增生产浏览器用例使用真实合成分析、SQLite 保存和正式 `open_project`：默认不导出与显式导出均
  保留正确的新 Program/Final exact、有序 raw 直连、真实可选 Crop；默认 0.3.5 和原素材不变。

固定本轮 production build 后：浏览器主套件 32 passed（4.1 min）、B 8 passed（1.4 min）、
C 6 passed（31.2 s），合计 46 项通过。只读预览 20 次点击反馈 p95 32.3 ms，仍不是媒体吞吐。
npm audit 0 漏洞。

从干净 `0534a7beda64d3594a0f232ff0d2b169ab94a974` 构建 Windows 候选，
`BUILD-INFO.json` 如实为 `working_tree / git_dirty=false`。原包及 ZIP 解压目录独立审计通过：
122 PYZ 模块、123 源码文件、7 production assets、4 媒体/许可输入精确匹配；解压前后 1,251 个文件
名称与 SHA-256 全部一致。交付 ZIP 208,757,519 B；具体路径和摘要见交付说明。

实际解压 exe 的独立本机状态首启、二次启动同实例、未确认退出拒绝、正式退出、重开新实例与密度偏好
保留通过；候选服务提供两项新融合定义。Chrome 在线首页显示 0.3.6，取消退出后可进入向导，确认退出
后显示正式关闭且精确进程/测试端口消失；四个测试标签关闭，原有标签保留。没有操作真实工程或媒体。
最后一次包审计仍通过。仅交付部分完成，操作者全流程、真实 AI 和长片/NAS 性能仍未确认。

源码冻结提交已推送；其 CI `35854152898`（Core）和 `35854152849`（Studio）记录时仍在运行。
交付后文档补记不改变程序或包身份；不创建 tag/Release，不切默认，不自动删除旧数据。
