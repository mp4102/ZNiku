# ZNIKU Studio v0.3.1 UI 验收记录

- 日期：2026-09-09。
- 当前范围：**实施前准备与本地门禁已完成；远端退出条件由同一提交 CI 核验；A/B/C 尚未开始。**
- 已批准安排：[v0.3.1 UI 优化执行方案](v0.3.1-ui-optimization-plan.md)，只按实施前门禁 → A → B → C 推进。
- 起点：`62d643f`；产品 `0.2.0`、wire/Presentation/StudioState `0.3.0`、SQLite schema 4 与 exact
  NodeDefinition 版本保持不变。
- Graph/Runtime 以上位 [Graph Core](architecture/graph-core-baseline.md) 为准，展示与交互继续遵守
  [Studio UX](architecture/studio-ux-baseline.md)。本记录不是另一份运行或参数合同。

## 1. 起点与范围

用户已批准全部详细 UI 方案，并授权本轮完成实施前门禁后 commit/push。原先未提交的方案、README 和
提案入口一并正式定稿；没有把后续 A/B/C 视为本轮授权，没有提前发布产品或迁移真实工程。

原 `62d643f` 的 Windows 测试通过不等于远端全绿：

| 起点门禁 | 已核实结果 | 前置处理 |
| --- | --- | --- |
| Core CI `34218031069` | 938 passed / 1 failed / 3 skipped | 链接逃逸由 Core 提前拒绝，与导入层测试预期错误不符 |
| Studio CI `34218031105` | 387 单测、类型/Schema/构建通过；生产 E2E 6 failed / 1 passed | 修复 POSIX 路径响应兼容，单独诊断 Submit 等待问题，补受控失败证据 |
| 本地 Windows 起点 | Python 941 passed / 1 skipped；Studio 387 单测、7 E2E 通过 | 保留并重跑，不以 Windows 结果替代 Linux CI |

旧失败记录保留，不追改为通过。新测试、选型和远端结果在本记录中追加实际证据。

## 2. 唯一状态所有者：核对源码后的迁移约束

本节记录实现起点的事实与后续 A 的迁移顺序；本轮没有搬动工作区组件或新增运行控制区。

| 状态或动作 | 当前唯一所有者 | 后续视图重组的边界 |
| --- | --- | --- |
| 当前 Graph、StudioState、撤销与保存 | `useAuthoringProject` → `EditHistory` / `AuthoringSaveController` | 保留同一 document/session；子面板不得再持有独立可保存 Graph |
| 参数编辑 | `StudioWorkspace` 的 `parameterDraft`、`parameterText` 与 `selectionGuardRef` | 表单和 raw JSON 是同一草稿；设置/文件/诊断页签只能共享，不能各初始化一份 |
| 当前/历史选择 | `viewRunId`、`showRunSnapshot`、`setTrustedViewRunId`、`selectRun` | 只切换视图；打开默认当前完整图，显式历史只读，不改 Run snapshot |
| 轻量状态与项目投影 | Workspace 的 status 轮询 effect、`acceptStatus`、generation/sequence | 抽屉复用投影，不增加另一个轮询或状态数据库；迟到响应不替换编辑 |
| 选中 Run 详情 | `loadDetail` 与 detail 轮询 effect | 按 Run/resource 合并请求和退避，拒绝不匹配 generation、Run 或回退状态 |
| 历史分页 | `loadOlderRuns`、`historyCursorRef`、独立 sequence | 有界读取；新 status 的同一 Run summary 优先，切换后旧页不能回写 |
| 外部检查和 Submit | `loadReadiness`、`checkedOutputs`、`handoffActionRef`、`handoffIsCurrent` | 正式服务完整重验；检查通过不等于完成，页面不自动 Submit |
| 外部文件选择/复制 | `useHandoffImport` 的 scope、flight、selection handle 和确认票据 | 原生选择无复制；显式确认后保留源；晚回执不能投给其他任务 |
| 收件观察与收纳 | `HandoffInbox` 与 Workspace 的 `inboxSubmissionFences` | Submit 时暂停旧观察，可信新 detail/空闲状态后恢复，不能只改按钮外观 |
| 所有正式 command | `executeCommands` / gateway → Python | 重组组件只传原 callback；不让新顶栏、抽屉各自执行一套命令 |
| 运行/重跑准备 | `saveThenRun`、`requestRerun`、`confirmRerun` 与 `flushAuthoring` | 保存门禁和影响预览仍绑定当前 session/CAS；确认后才调用正式命令 |
| 节点测量、拖动与坐标保存 | `GraphCanvas` / `use-canvas-node-geometry`；authoring 的 `begin` / `end` | 测量缓存只在 session；一次手势一次 Undo/保存，不因轮询换 identity |

以上标识对应 [StudioWorkspace](../apps/studio/src/studio/StudioWorkspace.tsx)、
[authoring hook](../apps/studio/src/studio/use-authoring-project.ts)、
[文件导入 hook](../apps/studio/src/studio/use-handoff-import.ts) 和
[画布测量 hook](../apps/studio/src/studio/components/use-canvas-node-geometry.ts)。

### 2.1 操作资格不能随按钮搬迁改变

| 操作 | 正式绑定/前置条件 | 不能增加或省略的行为 |
| --- | --- | --- |
| 运行当前 Graph | 未应用参数处理、保存 flush、session/CAS、Python 完整校验 | 不带旧参数启动；不将 UI 检查替代 GraphValidator |
| 打开/创建/替换工程 | 未应用草稿保护，既有保存与明确放弃流程 | 不以导航暗中丢弃草稿 |
| 查看历史 | `changeSelection` 的草稿保护、generation、Run ID、只读 snapshot | 不执行命令，不将所选历史写成新的活动 Run |
| 外部文件预览/确认 | Project session、Run、NodeRun、handoff、port、ordinal、selection handle | 不接受前端拼接的 raw target；原文件不移动/删除 |
| 检查/提交已有 handoff | 当前绑定、健康通道、完整检查、前后产物观察一致、显式 Submit | 不增加“下一次处理的 ParameterDraft 必须先保存”这一新资格 |
| 重跑预览/确认 | 当前图 flush、Run/node、session/CAS、generation 与影响清单 | 只从头创建新 attempt，不把按钮叫作节点内部 resume |
| 放弃本次处理 | 现有服务资格与明确任务选择 | 不顺便删媒体，不成为节点 Inspector 末尾的隐蔽整批操作 |

当前代码以 `viewRunId` 加载单份 detail；活动待办摘要来自正式 `run_summaries`，两者不是同一个含义。
后续任务抽屉可在查看旧历史时显示当前待办导航，但不能把旧历史 detail 当成待办详情，也不能再启动一份
无界 detail 轮询。点击待办后才切换精确目标，状态所有者仍在 Workspace。

旧历史 completed、另一任务 waiting、当前图有未应用参数三者共存时：顶栏动作归被查看历史，任务行保留
另一个活动待办的标题和导航，草稿不丢失。切换节点仍可被现有草稿保护阻止；这是导航约束，不是新的
Runtime Submit 资格。A 必须为此组合补集成测试。

### 2.2 批次 A 的组件迁移顺序

1. 固定上述 session/资源绑定和原命令 callback；先补行为回归，不复制状态所有者。
2. 从 `RunCenter.tsx` 拆出只消费 props 的主操作、历史、问题与任务摘要视图。
3. 将 `ProjectShell` 改为占位布局；同一批替换 `RunCanvasOverlays`，不保留新旧两个常驻控制区。
4. `NodeInspector` 建立稳定页签，参数/导入/收件控制器仍由当前所有者传入；页签切换不重置草稿。
5. 整理菜单与任务文案，精确动作继续回调原 command，并保留禁用原因与错误定位。
6. 将高级精确视图和日志展开解耦，错误摘要不受折叠影响；有界日志读取仍由原 resource key 控制。
7. 运行当前/历史、草稿、外部导入/Submit、CAS、断连和迟到响应组合回归后，才删除旧展示分支。

这是后续实现依据，不是本轮已经完成上述七步。

### 2.3 起点门禁的最小修复

- 首个提交 `9ed250a` 未改 Core 生产代码。链接逃出 attempt 时保留 `E_HANDOFF_RELATION_CORRUPT`；仍在 attempt 内的链接由
  导入层以 `E_HANDOFF_IMPORT_PATH` 拒绝。preview/confirm 分开验证，源、旧目标与持久状态不改变。
- Studio HostBridge 只修正正式服务响应中的绝对路径识别，兼容 POSIX 与既有 Windows 盘符/UNC；相对路径、
  URL、空白和 NUL 仍拒绝。真实路径授权、选择票据和 containment 仍由 Python 验证，不新增 Linux 原生窗口支持。
- Submit 仍保留原 15 秒完成断言，额外断言 exact Run/NodeRun/handoff 请求及 HTTP 200；失败时保留有界
  最终 Run/节点状态，避免仅凭 `Run.running` 把失败节点的可重试状态误判为 worker 无响应。
- 公开 CI 只上传脱敏 JSON 和遮罩截图，保留 7 天；不上传原始 trace、HAR、工程、日志或选择票据。短凭据、
  quoted JSON、Windows/POSIX/UNC 路径、深层数据、悬挂响应和截图日志/路径区域均有负向检查。
- 首个提交前，本地全量 Studio 曾分别遇到两项初始化时序失败：把运行按钮/队列已出现误当作 ReactFlow 已挂载。
  测试改为等待实际节点后再执行本例动作，不加 sleep、不提高超时、不修改 Workspace。它不宣称已修复
  生产页面在初始化中提前点击导航的行为；该边界仍需 A 的真实交互回归。
- 本轮重新 audit 时出现 2 条 moderate 报告，对应 Vitest 与 `@vitest/mocker` 的同一开发工具漏洞。
  按 [维护者公告](https://github.com/vitest-dev/vitest/security/advisories/GHSA-82fw-gwwq-j7x9)，只将 Vitest
  `4.1.10 → 4.1.11` 并更新配套锁项；不执行广泛 `audit fix`，不升级生产依赖或关闭 audit 门禁。

## 3. 几何试验与基准采集

本轮只在隔离实验/测试代码中建立 G01–G10，比较现有 fixed-grid/smoothstep 与有界替代算法。
正式 `src` 不引用实验模块，节点卡片、自动布局与边渲染不在本轮换成新实现。

几何数据、求解缓存、版本与降级只用于 session 试验；不写入 Graph、parameters、StudioState 或 snapshot，
不新增 Reroute 节点、持久手工走线点或新的 wire。不以模拟几何时间冒充 DOM 交互性能，也不以 DOM DPR
模拟代替真实 Windows 高 DPI 验收。

### 3.1 几何选型已完成

选择尺寸感知分层布局与有界正交通道搜索，保留 React Flow，无新增依赖。共 31 项测试、20 个变体，
每变体 20 次重复通过；可解变体的端点错误、障碍相交、降级均为 0。G09 故意无解/超预算时的 3 条降级线
及相交如实保留，不算避障成功。

200 节点/196 边几何：基准 12 次相交，替代为 0；替代计算 p95 9.055 ms，缓存命中 196 条路线。
当前 6/8/16 端口的百分比定位每侧分别越界 1/3/11 个，增高卡片无法修复百分比越界。
这些数据只支持选型，不宣称生产画布已修复、任意图最优或 1000 Run UI 已经通过性能验收。

完整输入、独立相交审计、资源/时间上限、缓存失效、降级、许可证及体积边界见
[图形实施前试验报告](v0.3.1-graph-preflight.md)；可复现命令和数据在
[独立实验目录](../apps/studio/preflight/README.md)。实验的资源上限与 `version: 1` 不是新的领域合同。

### 3.2 当前生产 UI 截图与浏览器基准

使用真实 production 资源、Python Project Service / SQLite / Runtime 和临时合成工程；只有原生选择器使用
测试平台。截图没有注入替代 DOM 或新 UI，蓝灰色矩形是公开证据的隐私遮罩，不是产品布局。采集源码对应
首个提交 `9ed250a`；资源入口 `index-Da0FpSKY.js`，升级测试依赖后再次构建保持相同资源名。后续交接竞争
窄修不改变该批截图所记录的布局起点；不将旧资源 hash 冒充后续构建的 hash。

| 顺序与操作路径 | 已保存的生产截图 |
| --- | --- |
| 打开 50 节点合成工程，选择/调参/拖动测量后适应画布 | [创作者当前图](assets/v0.3.1-preflight/creator-current-50-masked.png) |
| 同一工程切换高级节点图 | [高级当前图](assets/v0.3.1-preflight/advanced-current-50-masked.png) |
| 打开 200 节点、1000 条历史工程，测量后适应画布 | [200 节点当前图](assets/v0.3.1-preflight/creator-current-200-masked.png) |
| 显式选择第二条合成历史，显示本次处理的只读图 | [历史 snapshot](assets/v0.3.1-preflight/history-masked.png) |
| 打开正式注册的 6/8/16 输出端口合成工程 | [端口越界及跨层连线](assets/v0.3.1-preflight/ports-6-8-16-masked.png) |
| 独立合成源故意缺失，等待浏览器失败卡出现后选择节点 | [实际节点失败](assets/v0.3.1-preflight/failed-masked.png) |
| 运行双分支外部任务，等待两个 handoff 后选择 B | [等待外部处理 B](assets/v0.3.1-preflight/waiting-selected-B-masked.png) |
| 同一等待任务改为 1280×720 CSS 视口 | [窄窗口](assets/v0.3.1-preflight/narrow-1280x720-masked.png) |

历史夹具的 snapshot 是合法空图，当前 Graph 仍有 200 节点；空白历史截图不是丢图证明。适应画布会触发
原有 UI 视口保存，因此部分截图在“有未保存更改/正在保存”时刻采集，不据此宣称历史 snapshot 被写入。
6/8/16 端口使用合法合成 NodeDefinition，但没有专用 Presentation；截图中的隔离提示是通用回退情形。
它们不创建真实输出、注册 Artifact 或伪造媒体完成状态。

测量环境：Windows headless Chromium、production build、1920×1080 CSS 视口；窄窗口为 1280×720。
每项 20 个样本，从浏览器捕获输入事件至第二次 `requestAnimationFrame`，是既有交互响应指标，不是保存
往返耗时或完整任务耗时。参数每次先折叠，再只测展开；不把折叠与展开混算。

| 当前节点数 | 选择 p95 | 参数展开 p95 | 拖动 p95 |
| --- | --- | --- | --- |
| 50 | 31.7 ms | 31.6 ms | 18.1 ms |
| 200 | 30.5 ms | 31.1 ms | 18.0 ms |

本次 50 节点指标满足原 ≤150 ms 阈值，不表示复杂交互、真实 Windows DPI 或批次 B 已通过。
1000 条历史首屏 status/选择器均为 20 条，有下一页游标；没有把 1000 条历史传给几何实验。

真实 DOM 观察确认：6/8/16 输出分别有 1/3/11 个锚点越出卡片；50/200 图中，路径采样分别观察到
39/49、159/199 条边穿过其他卡片。此处是 SVG 路径采样观察，独立几何实验才使用解析相交判定。
等待视图的两个 absolute 横条均遮到 B 节点；1920 宽时各宽 1332 px、高约 38/64 px；1280 窄窗口中
宽 772 px，进一步遮到 A 分支。三个常规视口未出现整页横向 overflow，仍不等于画布没有遮挡或裁切。
这些都是 A/B 要消除的起点缺陷，不新增永久豁免。

完整机器观测见 [preflight-observation.json](assets/v0.3.1-preflight/preflight-observation.json)。重采命令：

```powershell
# cwd: apps/studio；按锁安装并完成生产构建后
npx playwright test -c e2e/preflight.config.ts
```

独立采集 1/1 通过。它记录当前缺陷，不计入正式 7 条生产闭环，也不取代 A/B/C 验收。测试设置工程前先
卸载旧合成页面，避免旧页面视口自动保存与夹具初始化命令竞争；没有放宽实际保存/CAS 失败语义。

## 4. 验证与当前退出条件

本地环境：Windows、Python 3.13.12、Node 24.14.0；远端为 Ubuntu / Python 3.12，不能互相替代。

| 检查 | 首个提交 `9ed250a` 的本地结果 |
| --- | --- |
| Python 全量 pytest | 951 passed / 4 skipped，404.49 秒；4 项为真实 symlink 创建权限限制 |
| 无特权路径安全回归 | preview/confirm、目标/父目录、symlink/reparse 与 Core containment 均执行；不以跳过代替安全验证 |
| 独立 generic media smoke | 通过：12 帧输入、两路 12 帧输出、完成节点复用 |
| 独立 AV27 synthetic E2E | 通过：24→48 帧、60000/1001、2 条原音轨、同一生产 Run、13 节点复用；109.81 秒 |
| lock / 生成 Schema | 通过；无合同或版本漂移 |
| Ruff / 格式 / mypy | 118 文件，Windows/Linux 两种静态目标均通过 |
| 架构、文档链接与产品版本 | 6 项通过；产品、wire、definition、数据库版本均保持 |
| 锁定依赖重新安装 | `npm ci` 通过；仅 Vitest 及配套 `@vitest/*` 锁项升级为 4.1.11 |
| Studio 类型 / 单测 / 构建 | 41 文件、403 测试全通过；typecheck、生产 build 通过 |
| npm audit | `--audit-level=low` 通过，0 vulnerabilities；没有永久豁免 |
| 几何独立门禁 | 31/31；完整 20 变体基准重跑退出 0，未被 Vitest 重复执行 |
| 最终 production E2E | 新锁文件/生产构建后 15/15，约 1.7 分钟；7 条生产流程 + 8 条诊断卫生 |
| 独立当前 UI 采集 | 1/1，8 张已检查的合成遮罩 PNG、机器观测、50/200 节点基准 |
| Git/产物卫生 | diff --check 通过；截图和 JSON 不含真实工程、媒体、个人路径或票据；无构建/运行缓存入库 |

本地实施前准备的七项退出材料均已齐备；最后的远端退出条件必须在推送后核对：包含本记录的同一提交在
[Core CI](https://github.com/mp4102/ZNiku/actions/workflows/contract-ci.yml) 与
[Studio CI](https://github.com/mp4102/ZNiku/actions/workflows/studio-ci.yml) 都通过，才可宣布整道门禁关闭。
此表不预先替未来 Actions 填写通过，也不以 Windows 结果代替 Ubuntu；若任一失败，仍须停在门禁修复。

旧 Submit 的 Ubuntu `running` 失败缺少当时节点/worker 证据，原因无法回溯；本轮未伪称其已由 POSIX
修复解释。Windows 连续三次独立闭环和最终完整 E2E 已通过，新增诊断使远端再次失败时可定位真实状态。

### 4.1 首次推送后的真实反馈

`9ed250a` 的 [Core CI 34313178639](https://github.com/mp4102/ZNiku/actions/runs/34313178639) 全部通过：
Python **952 passed / 3 skipped**（532.38 秒）、两条独立 smoke、Schema、mypy 118 文件、Ruff 与格式。
这关闭了起点 Linux 链接拒绝测试缺口；后续并发读取修复仍须在其最终提交重新完成门禁。

`9ed250a` 的 [Studio CI 34313178678](https://github.com/mp4102/ZNiku/actions/runs/34313178678)：
Schema、几何 31 项及 benchmark、类型、403 单测和构建通过；production E2E **14 passed / 1 failed**，
独立 audit 因前置失败未执行，不能宣称该提交远端全绿。原工程数据 Submit 流程本次 Ubuntu 已通过。

唯一失败是同名外部任务用例的额外 HTTP 409。受控诊断保留了精确 `handoff-readiness` 与
`E_SERVICE_HANDOFF_SUPERSEDED`，并证实两次 Submit 成功、Run 与 4 个节点全部 completed，且没有 rerun。
这使问题从“泛泛的运行失败”收敛为“终态期间旧被动检查与 Submit 竞争”。原始失败包不提交仓库。

源码核对发现 `inspect_external_handoff` 先读 Run、再单独读 NodeRun；两个读取时刻之间状态可更新，
完整对象比较把同一 attempt 的状态更新误判为替代。后续修复只处理一致的只读快照与 UI 被动轮询栅栏；
不增加新合同、改变 Runtime 状态机、放宽完整检查或过滤未预期 409。

### 4.2 交接竞争的修复边界

- Runtime 在 `get_run()` 的同一个 SQLite 读取快照中解析精确 NodeRun 与 latest attempt，按身份判断
  替代；快照之外的补查只区分未知/跨 Run，不能把更晚的对象混入旧快照。同 Run 的后生 attempt 仍拒绝。
- 只读观察可能在返回前已经过时，因此不作为登记许可。Repository 原有写事务继续重验父 Run、latest、
  waiting 与输出；未知 Run/NodeRun、跨 Run、真旧 attempt、非 waiting 和错误 handoff 的失败语义保留。
- Workspace 在本地交接动作、command、服务 active operation 或该 handoff 的暂停期间不启动新的被动
  readiness。解除暂停必须依赖空闲时发起并返回的可信 detail，不能把旧 status 的 idle 当成本地 Submit 完成。
- 已发出的被动 HTTP 仍有物理请求记录。Submit 先使其观察失效并等待结束，最多 5 秒；未结束就停止本次
  用户操作，不发 Submit、不自动重试、不假装旧请求取消。排空后仍执行原完整 probe、产物一致性和精确绑定。
- 跨 Run 导航的被动物理请求总上限为 8，只有真正结束才释放名额；这是 UI 资源预算，不是 Runtime 并发合同。
  导入/收纳释放 busy 后触发原有 detail 周期刷新，不新增第二个轮询或让操作中的回调穿过暂停门禁。
- 这不是提高 E2E 超时或删除 409 断言；生产闭环仍不允许未预期请求错误，完整流程与负向用例一起重跑。

### 4.3 修复后的重新验证

下表针对包含只读快照修复和被动检查栅栏的源码；首轮失败证据保留，不追改历史 CI。

| 检查 | 修复后本地结果 |
| --- | --- |
| Python 全量 pytest | 958 passed / 4 skipped，408.57 秒；新增 7 项并发/绑定回归，跳过原因不变 |
| 两条独立媒体 smoke | 均通过；AV27 107.73 秒，24→48 帧、60000/1001、2 条原音轨、同一 Run、13 节点复用 |
| Ruff / 格式 / 双平台 mypy | 全部通过，118 文件；没有修改写入事务或 Runtime 状态机 |
| lock / Schema / 架构与版本 | 全部通过；架构、链接与版本 6 项，未新增字段或版本变更 |
| Studio 类型 / 单测 / 生产构建 | 全部通过；41 文件、409 测试，新增 6 项确定性交错与有界请求回归 |
| 完整 production E2E | 15/15，约 1.6 分钟；原 7 条生产流程与 8 条诊断卫生均执行，未过滤额外 409 |
| 同名外部任务独立重复 | `--grep '生产外部任务' --repeat-each=3`：3/3；24.6 / 23.2 / 23.1 秒，各次精确两次 Submit、无额外请求错误 |
| 生产资源 | `index-CawRUlhp.js` 271.68 kB / gzip 82.33 kB；graph chunk 未改变 |
| npm audit | `--audit-level=low` 通过，0 vulnerabilities |
| 独立几何门禁 | 31 项与 20 变体重跑通过；不覆盖原始基准记录，不接入生产画布 |
| 独立审查与进程卫生 | Core 与 Workspace 窄修无阻塞；合成 fixture 进程已全部退出，未结束用户服务 |

本地验证已完成。同提交远端退出条件仍按 §4 执行：必须核对本修复提交的 Core CI 与 Studio CI 均通过；
不能沿用 `9ed250a` 的 Core 成功，也不能因本地重复成功而省略远端 Studio 门禁。最终交付报告提供实际 CI 链接。

本轮没有访问、迁移、删除或重新运行用户真实工程与媒体，也没有清理 AppData attempts。A/B/C、真实
短媒体新 UI 闭环、原生窗口、首次用户及最终产品版本收敛仍属于后续相应批次。
